<%= form_for @question, remote: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :body %>
  <%= f.text_area :body %><br>
  <%= f.submit "Ask question", class: 'btn btn-danger' %>
<% end %>
